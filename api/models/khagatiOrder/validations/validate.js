/**
* Validation code for field shippingMethod on khagatiOrder
* @param {import("gadget-server").KhagatiOrderShippingMethodFieldValidationContext } context - All the useful bits for running this validation.
*/
export default async ({api, record, errors, logger, field}) => {
  // TODO: implement validation
  // access things like `record.shippingMethod` to get the fields's data
  // add to the `errors` with `errors.add('shippingMethod', "did not pass the validation")`
};